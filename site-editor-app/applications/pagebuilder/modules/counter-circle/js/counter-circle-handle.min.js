(function( exports, $ ) {
    var api = sedApp.editor;

    $( function() {

        $('.counter-circle-item').livequery(function(){
            var id_box = $(this).attr("id");

            var elm = this ,
                is_first_time = true;

            $("#" + id_box).parent().waypoint(function() {
                if( is_first_time === true ){
                    $("#" + id_box).circliful();
                    is_first_time = false;
                }
            }, {offset: 'bottom-in-view'});
        });
    });

}(sedApp, jQuery));