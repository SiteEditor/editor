(function( exports, $ ) {
    var api = sedApp.editor;

    $( function() {

        api.shortcodeUpdate.sed_slide_img = {

           /*description : function( id , value ){
                $('[sed_model_id="' + id + '"]').find("p").text( value );
            },*/

            alt : function( id , value ){
                $('[sed_model_id="' + id + '"]').find("img").attr( "alt" , value );
            },

            /*title : function( id , value ){
                $('[sed_model_id="' + id + '"]').find("h3").text( value );
            },*/    
        };

    });


}(sedApp, jQuery));