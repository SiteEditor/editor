(function( exports, $ ) {
    var api   = sedApp.editor;
    $( function() {

        api.shortcodeCreate.sed_breadcrumbs = function( id , param ){
                 
            param.context.breadcrumbs = window._sedAppBreadcrumbs;
                           
            return param;
        };

    });
}(sedApp, jQuery));