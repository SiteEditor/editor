(function( exports, $ ){
    
    var api = sedApp.editor;



    $( function() {


        api( 'my_setting2', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting3', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting4', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting5', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting6', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting7', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting8', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting9', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting10', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting11', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting12', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting13', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting14', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting15', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting16', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting17', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting18', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting19', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting20', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting21', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting22', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting23', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        //Add Custom Html Code To site or custom page
        api( 'custom_html_code', function( value ) {
            value.bind( function( to ) {
                //alert(to);
            });
        });

        api( 'custom_css_code', function( value ) {
            value.bind( function( to ) { 

                $('#option-custom-css').remove();

                var styleElement = ( 0 === $('#live-custom-css').length ) ? $('<style>' , { id : 'live-custom-css'}) : $('#live-custom-css');

                //sanitize string => remove html tags
                to = to.replace(/(<([^>]+)>)/ig,"");

                if (  0 === $('#live-custom-css').length )
                    $('head').append( styleElement.html(to) );
                else
                    styleElement.html(to);

            });
        });


        api( 'my_setting25', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting26', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting27', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting28', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });
        

    });


})( sedApp, jQuery );