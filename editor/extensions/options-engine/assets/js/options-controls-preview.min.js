(function( exports, $ ){
    
    var api = sedApp.editor;



    $( function() {


        api( 'my_setting', function( value ) {
            value.bind( function( to ) {
                alert(to);
            });
        });

        api( 'my_setting20', function( value ) {
            value.bind( function( to ) {
                //alert(to);
            });
        });

    });


})( sedApp, jQuery );